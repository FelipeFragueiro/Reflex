<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a0817moact03c_2.a0817moact03c_02">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Activities.DetalleGenerosDePeliculaActivity" />
        <activity android:name=".View.Activities.DetallePeliculaActivity" />
        <activity android:name=".View.Activities.FotoCompletaPeliculaActivity" />
        <activity android:name=".View.Activities.DetalleSeriesActivity" />
        <activity android:name=".View.Activities.GenerosActivity" />
        <activity android:name=".View.Activities.FavoritosActivity"></activity>
    </application>

</manifest>